[phases.setup]
nixPkgs = [
    "nodejs-18_x",
    "python3",
    "python3Packages.pip",
    "gcc",
    "nodePackages.pnpm"
]

[phases.install]
cmds = [
    "pnpm i --frozen-lockfile"
]

[phases.build]
cmds = [
    "pnpm run build"
]

[start]
cmd = "pnpm run start:prod"